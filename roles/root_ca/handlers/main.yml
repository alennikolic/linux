---
# roles/root_ca/handlers/main.yml
#
# Handler se poziva samo kada je CA sertifikat stvarno upisan u
# sistemski trust store ovog hosta.

- name: "azuriranje sistemskog trust store-a"
  ansible.builtin.command:
    cmd: update-ca-certificates
  changed_when: true
