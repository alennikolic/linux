---
# roles/timezone/handlers/main.yml
#
# Restart, ne reload — chrony nema reload koji bi ponovo procitao
# spisak izvora.

- name: "restart chrony"
  ansible.builtin.service:
    name: "{{ _timezone_chrony_service }}"
    state: restarted
