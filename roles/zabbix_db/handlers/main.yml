---
# roles/zabbix_db/handlers/main.yml

- name: "Restartuj bazu"
  ansible.builtin.service:
    name: "{{ _zabbix_db_service }}"
    state: restarted
  when: role_zabbix_db_service_state != 'stopped'
