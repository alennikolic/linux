---
# roles/zabbix_server/handlers/main.yml

- name: "Restartuj Zabbix server"
  ansible.builtin.service:
    name: "{{ _zabbix_server_service }}"
    state: restarted
  when: role_zabbix_server_service_state != 'stopped'
